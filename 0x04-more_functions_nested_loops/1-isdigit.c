#include "main.h"
/**
* _isdigit - Es un digito.
* @c: Caracter a chek
* Return: 1 if c is lowercase or 0 otherwise
*/

int _isdigit(int c)
{

if (c >= 48 && c <= 57)
{
return (1);
}
else
{
return (0);
}
}
