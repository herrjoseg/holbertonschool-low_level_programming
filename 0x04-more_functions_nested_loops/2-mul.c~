#include <stdio.h>
#include "main.h"

/**
* mul - Producto de dos variables
* @a: varriable a
* @b: variable b
* Return: num es el prodcuto de a * b
*/

int mul(int a, int b);
{

int num;
num = (a * b);
return (num);
}
