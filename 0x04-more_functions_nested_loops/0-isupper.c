#include "main.h"
/**
* _isupper - checks for upercase character.
* @c: Caracter a chek
* Return: 1 if c is upercase or 0 otherwise
*/

int _isupper(int c)
{

if (c >= 65 && c <= 90)
{
return (1);
}
else
{
return (0);
}
}
