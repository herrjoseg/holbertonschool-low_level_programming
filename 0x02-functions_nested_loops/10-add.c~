#include "main.h"
/**
* add -  adds two integers and returns the result.
* @a: sumando a
* @b: sumando b
* Return: devuelve la suma de a y+ b
*/

int add(int a, int b)
{

int resultado;

resultado=(a + b);
return (resultado);
}
